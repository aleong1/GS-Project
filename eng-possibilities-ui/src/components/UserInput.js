import React, { Component } from 'react';

/**
 * This component will display the user allocations for each category
 * 
 * Proptypes 
 * @param {(string, number) => void} updateAllocation updates the allocation for a specific category and it's corresponding percentage valye
 * @param {Map<string, number>} allocations a map keyed on the cateogory, which points to a percentage value
 */
class UserInput extends Component {
    

}

export default UserInput;